// هذا الـ script يوضح كيفية جلب بيانات المستخدم وتوافرها في نموذج الشراء

console.log(`
🧪 اختبار: ملء الاسم والهاتف تلقائياً

✅ الإصلاحات المطبقة:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1️⃣ useEffect في CartSidebar (السطر 439-451):
   - تم تحديث dependency array
   - الآن يتابع: [user?.id, user?.name, user?.phone, user?.customer_type]
   - بدلاً من: [user] فقط

   الكود الجديد:
   ┌─────────────────────────────────────────────────────────┐
   │ useEffect(() => {                                       │
   │   if (user?.name) {                                     │
   │     setName(user.name);  // تعيين الاسم مباشرة         │
   │   }                                                       │
   │   if (user?.phone) {                                    │
   │     setPhone(user.phone);  // تعيين الهاتف مباشرة       │
   │   }                                                       │
   │   ...                                                    │
   │ }, [user?.id, user?.name, user?.phone, ...])            │
   └─────────────────────────────────────────────────────────┘

2️⃣ الفرق الأساسي:
   ❌ القديم: if (user?.name && !name) - يتطلب أن يكون name فارغ
   ✅ الجديد: if (user?.name) - يعيّن القيمة مباشرة

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📋 كيفية الاختبار:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. قم بتسجيل دخول المستخدم في المتجر
   وتأكد من وجود:
   - اسم (name): محفوظ في قاعدة البيانات
   - هاتف (phone): محفوظ في قاعدة البيانات

2. اذهب إلى متجر الكارتات (topup store)

3. أضف منتج إلى السلة

4. انقر على "إتمام الشراء" / "عرض السلة"

5. تحقق من نموذج الشراء:
   ✅ يجب أن يكون الاسم مملوء تلقائياً
   ✅ يجب أن يكون الهاتف ممل تلقائياً

6. إذا كانت الحقول فارغة:
   - اختبر بـ browser DevTools Console
   - تحقق من: console.log({ name, phone, user })
   - تأكد من وجود user data

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🎯 النتيجة المتوقعة:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

عند الدخول إلى صفحة الشراء من متجر الكارتات:
- اسم العميل يظهر تلقائياً ✅
- رقم الهاتف يظهر تلقائياً ✅
- المستخدم يمكنه تعديل البيانات إذا أراد ✅
`);
